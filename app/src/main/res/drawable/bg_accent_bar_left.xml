<?xml version="1.0" encoding="utf-8"?>
<!-- Colored left accent bar on a list-item card (see UI_UX_Design/ reference) - only the left
     corners are rounded so it sits flush against the card's own rounded left edge. Tinted per
     instance at runtime via AccentColors.applyBar(), never used with its own default color. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/status_info" />
    <corners
        android:topLeftRadius="16dp"
        android:bottomLeftRadius="16dp"
        android:topRightRadius="0dp"
        android:bottomRightRadius="0dp" />
</shape>

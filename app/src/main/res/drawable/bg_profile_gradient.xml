<?xml version="1.0" encoding="utf-8"?>
<!-- Role-agnostic premium header gradient (the app's own Indigo brand color, light-to-dark -
     matches the same two-tone convention every per-role gradient already uses). Used by the
     shared self-service Profile screen, which every role opens the same way and shouldn't be
     colored by who's viewing it. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:type="linear"
        android:angle="135"
        android:startColor="@color/indigo_primary"
        android:endColor="@color/indigo_primary_dark" />
</shape>
